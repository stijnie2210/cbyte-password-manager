DATABASE_URL=postgres://cbyte:Cbyteiscool@localhost:5432/password_sharing
# 32-byte key, base64-encoded. Generate with: openssl rand -base64 32
SECRET_ENCRYPTION_KEY=
PORT=3000
# Comma-separated list of allowed frontend origins. Leave unset for local dev
# (allows all origins). In production, set to the deployed frontend URL,
# e.g. https://76f710-cbyte-frontend.container.tilaa.cloud
CORS_ORIGIN=
